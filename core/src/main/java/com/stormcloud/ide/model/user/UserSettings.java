package com.stormcloud.ide.model.user;

/**
 *
 * @author martijn
 */
public enum UserSettings {

    /**
     *
     */
    USER_HOME,
    /**
     * The project folder contains the 'opened' projects.
     */
    PROJECT_FOLDER,
    /**
     * The closed project folder contains the 'closed' projects.
     */
    CLOSED_PROJECTS_FOLDER,
    LOCAL_MAVEN_REPOSITORY,
    JAVA_HOME,
    TOMCAT_HOME,
    LOG_FOLDER,
    TRASH_FOLDER,
    FILE_TEMPLATE_FOLDER,
    DERBY_HOME,
    SSH_HOME
}
